@model IEnumerable<VideoGameSellers.ASP.Models.SaleModel>

@{
    ViewData["Title"] = "Index";
}
@Html.ActionLink("Créer une vente", "Create")
@foreach (var item in Model)
{
    <partial name="SaleView" model="item" />
}
