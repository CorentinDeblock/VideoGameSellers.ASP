@model VideoGameSellers.ASP.Models.MessageModel

<div class="bg-white p-2 rounded mt-2">
    <div class="d-flex justify-content-between">
        <h4 class="m-0">@Model.User.Username</h4>
        <partial name="OpinionView" model="Model.Opinions" />
    </div>
    <p class="m-0">@Model.Message</p>
    <p class="m-0">Publier le @Model.PublishedAt.ToLocalTime()</p>
</div>